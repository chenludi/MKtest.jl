Load packages
```julia
using MKtest
using CSV, DataFrames, JLD2
```
Parse sfs/div file
```julia
alpha, sfs, divergence = MKtest.parse_sfs(sample_size = 661, data = "./0416_tgp_largec_vipinfo.tsv",gene_list="../mammals_manual_VIPs.txt")
```
Calculate summary statistics
bootstrap equals to the number of gene list (rows) in gene_list file. Default value of bootstrap and threads is false.
```julia
adap = MKtest.parameters(n=661,dac=[1,2,4,5,10,20,50,100,200,400,500,661,925,1000])

@time summstat = MKtest.summary_statistics(param=adap,sfs=sfs,divergence=divergence,h5_file="../rates.jld2",analysis_folder="./orthovips/",summstat_size=10^5);
```
Plot
```julia
using RCall
include("../plot_map.jl")
vipsmap = plot_map(analysis_folder="./vip")
```
